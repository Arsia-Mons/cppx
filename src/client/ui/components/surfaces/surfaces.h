#pragma once

// Umbrella header for the shared "surfaces" component family. Re-exports the
// family's generated component headers so screens can include the family in one
// line. Add new surface components' generated headers here as they are created.

#include "client/ui/components/surfaces/panel.h"
