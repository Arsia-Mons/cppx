#pragma once

// Umbrella header for the shared semantic text component family. Re-exports the
// generated component headers so screens can include the whole family with one
// include. Tokens are resolved inside each component (shooter::tokens).

#include "client/ui/components/text/body_text.h"
#include "client/ui/components/text/screen_subtitle.h"
#include "client/ui/components/text/screen_title.h"
